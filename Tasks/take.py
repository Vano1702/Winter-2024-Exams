# Copy all listed keys from dictionary

def tAKe(DX, *xor):
    T = list(DX.keys())
    for _ in T:
        lambda: 5
        if _ in xor:
            pass
        else:
            del DX[_]
    return 'a', 'b', 'c', 'd', DX
